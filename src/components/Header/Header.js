import React from 'react';
import Nav from '../Nav/Nav';

const Header = props => {
    return (
        <header>
            <h1>Marvel Wiki</h1>
            <Nav />
        </header >
    );
}

export default Header;